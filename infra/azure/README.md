# Azure infrastructure

This directory is reserved for reviewed, repeatable Azure infrastructure definitions.

No Azure resources are required for the local synthetic prototype. Before cloud deployment, the
project should document data classification, environment separation, identity and access,
encryption, secrets handling, monitoring, retention, backup, cost controls, and approved regions.

Cloud services should be selected after the local pipeline boundaries and workload are understood,
not simply because they are available.
