# Notebooks

Use notebooks for disposable exploration and visual checking only. Production transformations,
validation rules, and metric definitions must live in tested Python or SQL modules.

Notebooks must use synthetic data during the prototype phase.
