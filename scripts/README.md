# Scripts

Developer utilities will live here, including synthetic-data generation, local pipeline execution,
and output inspection.

Scripts must avoid hidden business logic. Reusable validation and transformation behaviour belongs
in `src/osna_pipeline/`.
